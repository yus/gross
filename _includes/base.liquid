<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | Gross Experiments</title>
  
  <!-- Pico CSS - FREE and beautiful! -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
  
  <!-- Custom gross styles -->
  <style>
    /* Gross accent colors - NAVY THEME */
    [data-theme=light] {
      --primary: #1a3b7a;               /* Navy blue */
      --primary-hover: #0f2a5c;         /* Darker navy */
      --primary-focus: rgba(26, 59, 122, 0.25);
    }
    
    [data-theme=dark] {
      --primary: #2a5298;               /* Brighter navy for dark mode */
      --primary-hover: #1a3b7a;
      --primary-focus: rgba(42, 82, 152, 0.3);
    }
    
    /* Gross header - NAVY with STATIC CRISPY DOTS */
    header {
      background: 
        /* FIRST DOTS LAYER - tighter */
        radial-gradient(circle at 3px 3px, 
          rgba(255, 255, 255, 0.25) 1px, 
          transparent 1px
        ),
        /* SECOND DOTS LAYER - offset */
        radial-gradient(circle at 9px 9px, 
          rgba(255, 255, 255, 0.15) 1px, 
          transparent 1px
        ),
        /* GRADIENT */
        linear-gradient(
          135deg,
          #0f1b3d 0%,
          #1a3b7a 25%,
          #2a5298 50%,
          #1a3b7a 75%,
          #0f1b3d 100%
        );
      
      background-size: 
        3px 3px,    /* TIGHT: 1px dot + 5px gap */
        3px 3px,    /* Same grid */
        300% 300%;  /* Gradient size */
      
      background-position: 
        0 0,        /* First dots */
        3px 3px,    /* Offset second layer by half grid */
        0% 0%;      /* Gradient start */
      
      position: relative;
      overflow: hidden;
      animation: gradientShift 15s ease infinite;
    }
    
    @keyframes gradientShift {
      0% { background-position: 0 0, 3px 3px, 0% 0%; }
      50% { background-position: 0 0, 3px 3px, 100% 100%; }
      100% { background-position: 0 0, 3px 3px, 0% 0%; }
    }
    
    /* Make dots more CRISPY (smaller, sharper) */
    header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      /* EXTRA CRISPY DOTS OVERLAY */
      background-image: 
        radial-gradient(circle at 5px 5px, 
          rgba(255, 255, 255, 0.2) 1.5px, 
          transparent 1.5px
        );
      background-size: 10px 10px; /* 1.5px dot + 8.5px gap = SUPER CRISPY */
      pointer-events: none; /* Don't interfere with clicks */
    }
    
    /* Text styling for dark background */
    header h1 {
      color: white;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.4);
      font-weight: 300;
      letter-spacing: 1px;
    }
    
    header p {
      color: rgba(255, 255, 255, 0.9);
      text-shadow: 0 1px 2px rgba(0, 0, 0, 0.3);
    }
    
    /* Navigation buttons */
    header nav a {
      background: rgba(255, 255, 255, 0.12) !important;
      border: 1px solid rgba(255, 255, 255, 0.25) !important;
      color: white !important;
      transition: all 0.2s ease;
    }
    
    header nav a:hover {
      background: rgba(255, 255, 255, 0.25) !important;
      transform: translateY(-1px);
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    }
    
    /* Gross card enhancements */
    .gross-card {
      border-left: 8px solid var(--primary);
      transition: all 0.3s ease;
      position: relative;
    }
    
    .gross-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }
    
    /* Gross tags styling */
    .gross-tag {
      background: linear-gradient(135deg, #f1f1f1, #e1e1e1);
      border: 1px solid #ddd;
      font-family: 'Courier New', monospace;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <!-- Gross Header -->
  <header>
  <div class="container">
      <!-- Add theme toggle button here -->
      <button onclick="toggleTheme()" 
              style="position: absolute; top: 1rem; right: 1rem; background: rgba(255,255,255,0.2); border: 1px solid rgba(255,255,255,0.3); color: white; padding: 0.5rem 1rem; border-radius: 0.5rem; cursor: pointer;">
        🌓 Toggle
      </button>
      <div class="logo-container">
        <div class="logo-svg">
          <img src="/gross/img/logo.svg" alt="Gross 144 Logo" style="width: 48px; height: 48px;">
        </div>
        <div>
          <h1 style="margin: 0; font-size: 1.5rem; font-weight: normal;">GROSS 144</h1>
          <p style="opacity: 0.9; margin: 0; font-size: 0.9rem;">is a digital tabloid</p>
        </div>
      </div>
      
      <nav style="position: relative; z-index: 10;">
        <ul style="display: flex; gap: 1rem; list-style: none; padding: 0; margin: 0;">
          <li style="position: relative;">
            <a href="/gross/" 
               style="display: inline-block; padding: 0.5rem 1rem; color: white; text-decoration: none; 
                      position: relative; z-index: 11;">
              Home
            </a>
          </li>
          <li style="position: relative;">
            <a href="/gross/archive/" 
               style="display: inline-block; padding: 0.5rem 1rem; color: white; text-decoration: none;
                      position: relative; z-index: 11;">
              Archive
            </a>
          </li>
          <li style="position: relative;">
            <a href="/gross/tags/" 
               style="display: inline-block; padding: 0.5rem 1rem; color: white; text-decoration: none;
                      position: relative; z-index: 11;">
              Tags
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </header>
  
  <!-- Main Content -->
  <main class="container">
    {{ content }}
  </main>

  <footer style="
    width: 100%;
    min-height: 48px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #ff6b6b 100%);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 3rem;
    padding: 0.75rem 1rem;
    text-align: center;
  ">
    <div>
      <div style="margin-bottom: 0.25rem;">
        <span style="opacity: 0.9; font-size: 0.9rem;">
          <strong>{{ collections.posts.size }}</strong> experiments • Gross 144
        </span>
      </div>
      <div style="font-size: 0.8rem; opacity: 0.7;">
        <a href="https://yusdesign.github.io/" style="color: white; text-decoration: underline;">yusdesign, L</a> • 
        <a href="archive/" style="color: white; text-decoration: underline;">Archive</a> • 
        {{ "now" | date: "%Y" }}
      </div>
    </div>
  </footer>

  <script>
    // Theme toggle
    function toggleTheme() {
      const current = document.documentElement.getAttribute('data-theme');
      const newTheme = current === 'dark' ? 'light' : 'dark';
      document.documentElement.setAttribute('data-theme', newTheme);
      localStorage.setItem('theme', newTheme);
    }
    
    // Load saved theme
    const savedTheme = localStorage.getItem('theme') || 'light';
    document.documentElement.setAttribute('data-theme', savedTheme);
  </script>
</body>
</html>
