<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | Gross Experiments</title>
  
  <!-- Pico CSS - FREE and beautiful! -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
  
  <!-- Custom gross styles -->
  <style>
    :root {
      --gross-pink: #ff6b6b;
      --gross-purple: #764ba2;
      --gross-blue: #667eea;
    }
    
    /* Gross header */
    header {
      background: linear-gradient(135deg, var(--gross-blue), var(--gross-purple), var(--gross-pink));
      color: white;
      padding: 3rem 1rem;
      margin-bottom: 3rem;
    }
    
    /* Gross cards */
    .gross-card {
      background: white;
      border-radius: 1rem;
      padding: 2rem;
      margin-bottom: 2rem;
      border-left: 8px solid var(--gross-pink);
      box-shadow: 0 10px 30px rgba(0,0,0,0.1);
      transition: transform 0.3s;
    }
    
    .gross-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 15px 40px rgba(0,0,0,0.15);
    }
    
    /* Gross tags */
    .gross-tag {
      display: inline-block;
      background: #f1f1f1;
      padding: 0.25rem 1rem;
      border-radius: 2rem;
      font-size: 0.875rem;
      margin-right: 0.5rem;
      margin-bottom: 0.5rem;
      font-family: 'Courier New', monospace;
    }
    
    /* Gross logo area */
    .logo-container {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1rem;
    }
    
    .logo-svg {
      width: 64px;
      height: 64px;
    }
  </style>
</head>
<body>
  <!-- Gross Header -->
  <header>
    <div class="container">
      <div class="logo-container">
        <!-- Your SVG logo -->
        <div class="logo-svg">
          {% include "logo.svg" %}
        </div>
        <div>
          <h1 style="margin: 0; font-size: 3rem;">GROSS 144</h1>
          <p style="opacity: 0.9; margin: 0;">A tabloid for digital experiments</p>
        </div>
      </div>
      
      <nav>
        <ul>
          <li><strong>Gross 144</strong></li>
        </ul>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="https://github.com/yus/gross">GitHub</a></li>
        </ul>
      </nav>
    </div>
  </header>
  
  <!-- Main Content -->
  <main class="container">
    {{ content | safe }}
  </main>
  
  <!-- Footer -->
  <footer class="container" style="text-align: center; margin-top: 4rem; padding: 2rem 0; border-top: 1px solid #eee;">
    <p>
      <small>
        Built with 11ty + Pico CSS • 
        <a href="https://github.com/yus/gross">View source</a> • 
        Updated {{ "now" | date: "%Y-%m-%d" }}
      </small>
    </p>
  </footer>
</body>
</html>
