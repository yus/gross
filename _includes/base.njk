<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} | Gross Experiments</title>
  
  <!-- Pico CSS - FREE and beautiful! -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
  
  <!-- Custom gross styles -->
  <style>
    /* Gross accent colors */
    [data-theme=light] {
      --primary: #ff6b6b;  /* Gross pink */
      --primary-hover: #ff5252;
      --primary-focus: rgba(255, 107, 107, 0.25);
    }
    
    /* Gross header enhancement */
    header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #ff6b6b 100%);
      position: relative;
      overflow: hidden;
    }
    
    header::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255,255,255,0.1) 1px, transparent 1px);
      background-size: 20px 20px;
      animation: grossGrid 20s linear infinite;
    }
    
    @keyframes grossGrid {
      0% { transform: translate(0, 0); }
      100% { transform: translate(20px, 20px); }
    }
    
    /* Gross card enhancements */
    .gross-card {
      border-left: 8px solid var(--primary);
      transition: all 0.3s ease;
      position: relative;
    }
    
    .gross-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }
    
    /* Gross tags styling */
    .gross-tag {
      background: linear-gradient(135deg, #f1f1f1, #e1e1e1);
      border: 1px solid #ddd;
      font-family: 'Courier New', monospace;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <!-- Gross Header -->
  <header>
    <div class="container">
      <div class="logo-container">
        <!-- Your SVG logo -->
        <div class="logo-svg">
          {% include "logo.svg" %}
        </div>
        <div>
          <h1 style="margin: 0; font-size: 3rem;">GROSS 144</h1>
          <p style="opacity: 0.9; margin: 0;">A tabloid for digital experiments</p>
        </div>
      </div>
      
      <nav>
        <ul>
          <li><strong>Gross 144</strong></li>
        </ul>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="https://github.com/yus/gross">GitHub</a></li>
        </ul>
      </nav>
    </div>
  </header>
  
  <!-- Main Content -->
  <main class="container">
    {{ content | safe }}
  </main>
  
  <!-- Footer -->
  <footer class="container" style="text-align: center; margin-top: 4rem; padding: 2rem 0; border-top: 1px solid #eee;">
    <p>
      <small>
        Built with 11ty + Pico CSS • 
        <a href="https://github.com/yus/gross">View source</a> • 
        Updated {{ "now" | date: "%Y-%m-%d" }}
      </small>
    </p>
  </footer>
</body>
</html>
